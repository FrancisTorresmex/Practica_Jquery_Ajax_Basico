Listado de cursos (Uso personal)

1- Selectores:
    
    Se revisa el como se pueden modificar elementos o textos de una etiqueta del html, 
    agregar clases, eliminar clases, llamar elementos etc. (attr, remove, addClass, css, hide etc.)

2- Eventos:

    Se revisan los eventos como click (click, keyup, submit)

3- Ajax_Jquery:

    Se ve el tema de usar Ajax junto a Jquery se reviso en el orden:
    $.get(), $.each(), json, keyup(), leer json que contiene objetos con listas cada uno

4- Ajax_Jquery_2:

    Se revisa las funciones AJAX(), el como realizar una petición get

5- Ajax_Jquery_php:

    Para correr esta parte se requiere un servidor, como XAMPP.
